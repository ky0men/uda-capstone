apiVersion: v1
kind: Service
metadata:
  name: uda-final
  labels:
    app: uda-final
spec:
  type: LoadBalancer
  selector:
    app: uda-final
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
